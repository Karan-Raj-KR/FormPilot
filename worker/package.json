{
  "name": "formpilot-sync",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir=dist",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute formpilot --remote --file=./schema.sql",
    "db:local": "wrangler d1 execute formpilot --local --file=./schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "wrangler": "^3.99.0"
  }
}
